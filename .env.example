# Extension dev server port (used by WXT/Vite for HMR)
DEV_SERVER_PORT=3000

# Slack app client ID (safe to include — PKCE means no client secret needed)
VITE_SLACK_CLIENT_ID=your_slack_client_id_here
